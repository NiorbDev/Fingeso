spring:
  application:
    name: pgt-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/pgt_usach}
    username: ${DB_USER:pgt}
    password: ${DB_PASSWORD:pgt2026}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    default-property-inclusion: non_null

server:
  port: ${PORT:8080}
  error:
    include-message: never

app:
  frontend-origin: ${FRONTEND_ORIGIN:http://localhost:5173}

